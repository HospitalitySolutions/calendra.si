Changes done:
- Added guest forgot-password backend flow with secure reset tokens, 60-minute expiry, rate limiting, and enumeration-safe response.
- Added guest backend endpoints:
  - POST /api/guest/auth/forgot-password
  - GET /api/guest/auth/reset-password/validate
  - POST /api/guest/auth/reset-password
- Added guest password reset email generation with mobile deep link calendra-guest://reset-password and fallback web reset URL.
- Added shared guest-mobile API models/repository calls for requesting, validating, and completing password resets.
- Added Android guest mobile "Ste pozabili geslo?" link on login, forgot-password email entry screen, email-sent confirmation state, reset-password screen, and deep-link handling.
- Added iOS guest mobile "Ste pozabili geslo?" link on login, forgot-password email entry screen, email-sent confirmation state, reset-password screen, and deep-link handling.
- Added Android manifest deep link route for calendra-guest://reset-password.
- Added iOS Xcode project references for new ForgotPasswordView and ResetPasswordView files.
- Updated web ResetPasswordPage to support guest fallback reset links with guest=1 using guest auth endpoints.

Build check:
- Backend build could not be run in this sandbox because Maven is not installed and ./mvnw cannot download Maven without internet access.
- Guest-mobile Android build could not be run because Gradle wrapper cannot download Gradle without internet access.
