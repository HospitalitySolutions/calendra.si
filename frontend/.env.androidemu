# Used by: npm run build:android:emu (Android Emulator → host PC)
# 10.0.2.2 is the emulator’s alias for the host loopback (no adb reverse needed).
# Optional: run `adb reverse tcp:4000 tcp:4000` and set VITE_API_URL=http://127.0.0.1:4000/api instead.
VITE_API_URL=http://10.0.2.2:4000/api
