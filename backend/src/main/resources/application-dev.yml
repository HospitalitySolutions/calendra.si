# Profile: dev — development environment.
# Keep the same migration-first lifecycle as local/staging/production so schema drift
# cannot be hidden by Hibernate before launch.
spring:
  flyway:
    enabled: true
    validate-on-migrate: true
    clean-disabled: true
    baseline-on-migrate: false
  jpa:
    hibernate:
      ddl-auto: validate
