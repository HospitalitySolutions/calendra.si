package com.example.app.settings;

/*
 * Removed: tenant-wide general app settings were rolled back.
 *
 * This empty compilation unit intentionally replaces the previous
 * TenantGeneralSettingsService so applying the patch over an existing checkout
 * removes the old public class without requiring a manual file delete.
 */
